import common from '../../public/locales/en/common.json';
import topbar from '../../public/locales/en/topbar.json';

export interface LocaleResources {
  common: typeof common;
  topbar: typeof topbar;
}
